import React from 'react';

const HubView: React.FC = () => {
  return (
    <div>
      <h2 style={{ fontSize: '2rem', fontWeight: 'bold', marginBottom: '1rem' }}>
        Hub Central
      </h2>
      <p style={{ color: '#64748b' }}>
        Gerencie todo o seu conteúdo em um único lugar.
      </p>
    </div>
  );
};

export default HubView;
